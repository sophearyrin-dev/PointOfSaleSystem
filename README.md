# Point_Of_Sale
```
NODE_OPTIONS=--openssl-legacy-provider ng serve
```
```js
"scripts": {
  "ng": "ng",
  "start": "NODE_OPTIONS=--openssl-legacy-provider ng serve",
  "build": "ng build",
  "test": "ng test",
  "lint": "ng lint",
  "e2e": "ng e2e"
}
```
